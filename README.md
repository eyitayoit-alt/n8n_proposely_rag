# Proposely n8n RAG Workflow.
This a Retrieval Augmented Generation(RAG) automation  using n8n form node, Postgres vector store, Chat trigger, AI agent, OpenAI chat model and embeddings. Allows user to generate cover letter and job proposal using relevant information from the user’s CV and job description.
